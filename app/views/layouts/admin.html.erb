<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
  <head> 
  <meta http-equiv="X-UA-Compatible" content="IE=7" />
  <meta http-equiv="content-type" content="text/html; charset=utf-8">
  <title>SOPAR - Control Panel</title>
  <%= stylesheet_link_tag 'blueprint/screen', :media => "screen, projection" %>
  <%= stylesheet_link_tag 'blueprint/print', :media => "print" %>
  <!--[if IE]>
  	<%= stylesheet_link_tag 'blueprint/ie', :media => "screen, projection" %>
  <![endif]-->
  <%= stylesheet_link_tag 'admin' %>
  <%= javascript_include_tag :defaults %>
  <%= yield(:head) %>
</head> 
<body>
	
  <div class="container" id="container">
	
    <div class="span-24 header">
        <h1>SOPAR - <em>Control Panel</em></h1>
    </div>

	<div class="span-24">
    <h3>
      <%= @current_user.username %> &gt;&gt;  
      <%= link_to "Pages", "/" %> |
      <%= link_to "Articles", "/" %> |
      <%= link_to "Projects", "/" %> |
      <%= link_to "Categories", admin_categories_url %> |
      <%= link_to "Preferences", edit_admin_site_pref_url(1) %> |
      <%= link_to "Profile", edit_admin_user_url(@current_user) %> |
      <%= link_to "Logout", "/logout" %>

    </h3>
  </div>

  <!-- page title -->
  <%= yield(:title) %>
	
	<!-- status messages -->
  <% unless flash.blank? %>
  	<div id="statusMessage" class="span-24">
    <% flash.each do |key, msg| %>
	<% cls = (key.to_s == "warning") ? "error" : "success"  %>
      <%= content_tag :div, msg, :class => cls %>
    <% end %>
	<% flash.discard %>
  	</div>
    <hr />
  <% end %>
  
  
  <!-- content -->
  <div id="content" class="span-24">
      <%= yield %>
      <div class="spacer">&nbsp;</div>
  </div>

</div>
<% if ENV['RAILS_ENV'] == 'development' %>
	<div class="span-24">
		<%#= render :partial => "shared/debug" %>
	</div>
<% end %>
</body> 
</html>
